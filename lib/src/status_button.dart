/// Enum representing the possible states of a loading status button.
///
/// This enum is used to define the various states that the button can be in,
/// such as loading, error, success, enabled, or disabled.
enum StatusButton {
  loading,
  error,
  success,
  enable,
  disable,
}
